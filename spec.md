# Specification

## Summary
**Goal:** Remove the Gallery section from the landing page.

**Planned changes:**
- Remove the `GallerySection` component from `App.tsx`
- Remove any navigation links pointing to the gallery section anchor in `SiteHeader` and `SiteFooter`

**User-visible outcome:** The landing page no longer displays a Gallery section, and all header/footer navigation links remain functional with no broken anchors.
